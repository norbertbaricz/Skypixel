<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#121212">
    <meta name="description" content="Meet the talented Skypixel team - passionate developers and creators building innovative projects and software solutions.">
    <meta name="keywords" content="Skypixel team, developers, MaxUltimat3, creative team, software developers">
    <meta name="author" content="Skypixel Team">
    <meta property="og:title" content="About Us - Skypixel Team">
    <meta property="og:description" content="Meet the passionate team behind Skypixel's innovative projects.">
    <meta property="og:type" content="website">
    <meta property="og:image" content="/images/logo.png">
    <title>About Us - Skypixel</title>
    <link rel="icon" type="image/png" href="/images/logo.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="preconnect" href="https://cdnjs.cloudflare.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css?v=<%= assetVersion %>">
    <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" media="print" onload="this.media='all'" integrity="sha512-Fo3rlrZj/k7ujTnHg4CGR2D7kSs0v4LLanw2qksYuRlEzO+tcaEPQogQ0KaoGN26/zrn20ImR1DfuLWnOo7aBA==" crossorigin="anonymous">
</head>
<body class="has-sticky-footer">
    <div id="loader-wrapper">
        <div class="loader-content">
            <div id="loading-text-animation"></div>
        </div>
    </div>

    <div class="page-blur" aria-hidden="true"></div>
    <div id="main-content">
        <nav class="navbar">
            <div class="container">
                <a href="/" class="nav-brand">Skypixel</a>
                <a href="/projects" class="btn"><i class="fas fa-briefcase"></i> View Projects</a>
            </div>
        </nav>
        <section id="about" class="about section-padding fade-in-scroll">
            <div class="container">
                <h2>MEET THE TEAM</h2>

                <% const teamCategories = teamData?.categories || { creator: [], contributors: [] }; %>

                <% if (teamCategories.creator.length) { %>
                    <div class="team-category category-lead">
                        <div class="category-header">
                            <h3>Creator</h3>
                            <p>Leading the vision and shaping every Skypixel project from the ground up.</p>
                        </div>
                        <div class="team-grid">
                            <% teamCategories.creator.forEach((member, index) => { %>
                                <div class="team-card fade-in-scroll" style="animation-delay: <%= index * 0.1 %>s;" data-team-card
                                    data-title="<%= member.name %>"
                                    data-role="<%= member.role %>"
                                    data-description="<%= member.description %>"
                                    data-image="<%= member.avatar %>"
                                    data-url="<%= member.profileUrl %>">
                                    <img src="<%= member.avatar %>" alt="<%= member.name %>" loading="lazy" decoding="async" onerror="this.onerror=null;this.src='https://placehold.co/200x200/252525/e0e0e0?text=Image+Error';">
                                    <span class="team-badge badge-lead">Creator</span>
                                    <h4><%= member.name %></h4>
                                    <p><%= member.description %></p>
                                    <div class="project-buttons">
                                        <a href="<%= member.profileUrl %>" class="btn" target="_blank" rel="noopener"><i class="fab fa-github"></i> GitHub</a>
                                    </div>
                                </div>
                            <% }); %>
                        </div>
                    </div>
                <% } %>

                <% if (teamCategories.contributors.length) { %>
                    <div class="team-category category-engineering">
                        <div class="category-header">
                            <h3>Contributors</h3>
                            <p>Everyone who contributed to Skypixel repositories and helped ship the projects.</p>
                        </div>
                        <div class="team-grid">
                            <% teamCategories.contributors.forEach((member, index) => { %>
                                <div class="team-card fade-in-scroll" style="animation-delay: <%= (index + 2) * 0.1 %>s;" data-team-card
                                    data-title="<%= member.name %>"
                                    data-role="<%= member.role %>"
                                    data-description="<%= member.description %>"
                                    data-image="<%= member.avatar %>"
                                    data-url="<%= member.profileUrl %>">
                                    <img src="<%= member.avatar %>" alt="<%= member.name %>" loading="lazy" decoding="async" onerror="this.onerror=null;this.src='https://placehold.co/200x200/252525/e0e0e0?text=Image+Error';">
                                    <span class="team-badge badge-engineering">Contributor</span>
                                    <h4><%= member.name %></h4>
                                    <p><%= member.description %></p>
                                    <div class="project-buttons">
                                        <a href="<%= member.profileUrl %>" class="btn" target="_blank" rel="noopener"><i class="fab fa-github"></i> GitHub</a>
                                    </div>
                                </div>
                            <% }); %>
                        </div>
                    </div>
                <% } %>

                <% if (!teamCategories.creator.length && !teamCategories.contributors.length) { %>
                    <div class="team-category category-engineering">
                        <div class="category-header">
                            <h3>No Team Members Found</h3>
                            <p>We couldn't load contributors from GitHub right now. Please check back soon.</p>
                        </div>
                    </div>
                <% } %>
            </div>
        </section>
        <footer class="sticky-footer">
            <p>&copy; 2025 Skypixel. All rights reserved.</p>
        </footer>
    </div>
    <script defer src="/js/script.js?v=<%= assetVersion %>"></script>
    <script defer src="/js/about-script.js?v=<%= assetVersion %>"></script>
</body>
</html>
