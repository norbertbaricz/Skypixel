# Server configuration
PORT=3000
NODE_ENV=development
TRUST_PROXY=false

# Optional GitHub API token to raise rate limits
GITHUB_TOKEN=

# Optional timeout (milliseconds) for GitHub API requests
GITHUB_TIMEOUT_MS=7000
